# argus-ui workspace — see repo root .gitignore for shared rules.
# Only workspace-unique rules live here.

# Vite's local-override convention (e.g. `config.local.json`, `vite.config.local.ts`).
# Kept scoped to this workspace so we don't accidentally ignore `*.local`
# files elsewhere in the repo.
*.local

# Vim swap files (.swp, .swo, .swn).
*.sw?
