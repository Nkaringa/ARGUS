name: Deploy to Production

on:
  push:
    branches: [main]

jobs:
  deploy-backend:
    name: Redeploy hermes
    runs-on: self-hosted

    steps:
      - name: Pull latest and rebuild
        run: |
          cd ~/ARGUS
          git pull origin main
          docker compose up -d --build hermes
          docker compose ps
